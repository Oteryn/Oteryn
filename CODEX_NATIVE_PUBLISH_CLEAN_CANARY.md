# Codex Native Publish Clean Canary

This file exists only to test native Codex existing-PR publishing with no manually supplied GitHub credential.

CANARY_VALUE=before

Expected repair: change only `CANARY_VALUE=before` to `CANARY_VALUE=after`.
